[
  { "query": "decide which files are most relevant by mixing closeness in meaning with exact word hits", "expected": ["src/search/hybrid.ts"] },
  { "query": "turn a snippet of code into the numeric form used for similarity lookups", "expected": ["src/embeddings/embed.ts"] },
  { "query": "nudge source implementations above their tests and let widely-imported files rise", "expected": ["src/search/hybrid.ts"] },
  { "query": "break a document into pieces along function and heading boundaries", "expected": ["src/indexing/chunker.ts"] },
  { "query": "detect that a file changed and only re-process the parts that differ", "expected": ["src/indexing/indexer.ts"] },
  { "query": "set up the tables and on-disk storage where everything is persisted", "expected": ["src/db/index.ts"] },
  { "query": "figure out which file an import statement actually points to", "expected": ["src/graph/resolver.ts"] },
  { "query": "read the assistant's saved dialogue history off disk", "expected": ["src/conversation/parser.ts"] },
  { "query": "automatically notice when a developer edits a file and refresh after a short pause", "expected": ["src/indexing/watcher.ts"] },
  { "query": "start the background service that exposes capabilities to the editor over a pipe", "expected": ["src/server/index.ts", "src/cli/commands/serve.ts"] },
  { "query": "load the user's settings and fall back to sensible defaults", "expected": ["src/config/index.ts"] },
  { "query": "find everywhere a given name is referenced across the whole project", "expected": ["src/search/usages.ts"] },
  { "query": "save a memorable milestone so a later session can recall what happened", "expected": ["src/db/checkpoints.ts"] },
  { "query": "attach a sticky note to a piece of code that survives between sessions", "expected": ["src/db/annotations.ts"] },
  { "query": "keep tabs on what people looked for and surface the ones that found nothing", "expected": ["src/db/analytics.ts"] },
  { "query": "the low-level keyword and nearest-neighbour lookups against the store", "expected": ["src/db/search.ts"] },
  { "query": "open a file, work out its kind, and pull out any leading metadata block", "expected": ["src/indexing/parse.ts"] },
  { "query": "measure retrieval quality with recall and reciprocal-rank style numbers", "expected": ["src/search/benchmark.ts"] },
  { "query": "compare answers produced with and without the extra retrieved context", "expected": ["src/search/eval.ts"] },
  { "query": "first-time onboarding that wires the editor and lays down configuration", "expected": ["src/cli/commands/init.ts"] },
  { "query": "diagnose why the environment is broken on someone's machine", "expected": ["src/cli/commands/doctor.ts"] },
  { "query": "ingest past chat sessions so their contents become findable", "expected": ["src/conversation/indexer.ts"] },
  { "query": "remember how far we have already read into each chat session", "expected": ["src/db/conversation.ts"] },
  { "query": "keep a file's pieces and their numeric forms in the store", "expected": ["src/db/files.ts"] },
  { "query": "record which module depends on which as connections between them", "expected": ["src/db/graph.ts"] },
  { "query": "show what is uncommitted in the working tree and the recent history", "expected": ["src/tools/git-tools.ts"] },
  { "query": "the capability surface for leaving and fetching notes on code", "expected": ["src/tools/annotation-tools.ts"] },
  { "query": "undo the setup and strip the integration out of a project", "expected": ["src/cli/commands/cleanup.ts"] },
  { "query": "refuse to operate on dangerous locations like the home or root folder", "expected": ["src/utils/dir-guard.ts"] },
  { "query": "consolidate neighbouring pieces from the same parent into one result", "expected": ["src/search/hybrid.ts"] }
]
