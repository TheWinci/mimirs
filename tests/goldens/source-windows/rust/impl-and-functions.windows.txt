tests/fixtures/source-windows/rust/impl-and-functions.rs
├── struct Report [1–5]
│   └── window [1–5] (108 chars) “/// A report assembled from ordered lines. pub struct Report { title: String, lines: Vec<String…”
├── impl Report [7–21]
│   ├── window [7–14] (226 chars) “impl Report { pub fn new(title: impl Into<String>) -> Self { Self { title: title.into(), lines:…”
│   └── window [15–21] (197 chars) “pub fn render(&self) -> String { let mut output = vec![self.title.clone()]; output.extend(self.…”
└── function normalize_title [23–25]
    └── window [23–25] (82 chars) “pub fn normalize_title(value: &str) -> String { value.trim().to_uppercase() }”
