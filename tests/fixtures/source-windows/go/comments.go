package windows

// RetryDelay is the delay used by the scheduler.
const RetryDelay = 250

// Initialize schedules the initial task.
func Initialize(task string) string {
	return task
}

// This final comment is intentionally standalone.
