/** This documentation belongs to the declaration below. */
export function documented(value: number): string {
  return String(value);
}

// This is a standalone source comment.

function previous(): void {} // This trailing comment remains with previous.
export function next(): void {}
