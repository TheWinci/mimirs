/** Convert a value into display text. */
export function documented(value) {
  // Keep conversion explicit for callers.
  return String(value);
}

/* Configuration shared by this module. */
export const defaultValue = 1;
