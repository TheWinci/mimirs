---
name: public-api
useWhen: The page documents concrete exports with ≥1 signature. Prefer per-file-breakdown over this for module pages with ≥3 files and ≥5 exports.
---

## Public API

```<lang>
<full signatures for each export, straight from the prefetched exports
array. Include doc comments when present. Do not paraphrase.>
```

<Short prose per export (1-2 sentences) describing what it does and when
callers use it. Skip the prose for pure types/interfaces where the
signature is self-describing.>
