---
name: usage-examples
useWhen: The prefetched `usageSites` list has ≥1 entry AND at least one call site is idiomatic (not a trivial import). Skip when usages are all re-exports or test fixtures.
---

## Usage

<1-2 sentences describing how callers use this in practice — not a list
of paths, but the shape of the call.>

```<lang>
<short, representative call-site snippet, quoted verbatim from one of the
usage sites when possible>
```

<Optional list of notable call sites if the usage shape varies>:

- `<path>:<line>` — <short note on how this site uses it differently>
