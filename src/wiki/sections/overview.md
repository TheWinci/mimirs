---
name: overview
useWhen: Every page. Leads with 1-2 sentences naming the concrete purpose and including the source path inline.
---

<1-2 sentence overview. Name the module/file's actual purpose — not
"this contains helpers". Include the source path inline: "The search
module (`src/search/`) implements hybrid ranking over FTS + vector
candidates." Not a separate "Source:" label line.>
